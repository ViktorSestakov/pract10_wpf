<Page x:Class="prakt8_wpf.pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:prakt8_wpf.pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="600"
      Title="MainPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="110"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Margin="5" Width="75" Height="30" Click="Create_Click">+</Button>
            <Button Margin="5" Width="100" Height="30" Click="Reseption_Click">Начать прием</Button>
            <Button Margin="5" Width="65" Height="30" Click="Сhange_Click">Изменить</Button>
            <Button Margin="5" Width="65" Height="30" Content="🗑" Click="Delete_Click" x:Name="delete"></Button>

        </StackPanel>
        <ListView Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Label Content="Текущий прользователь:" FontSize="20" Grid.Column="1" Grid.Row="0" Grid.ColumnSpan="3" VerticalAlignment="Bottom"/>
        <StackPanel Grid.Column="1" Grid.Row="1">
            <Label Content="ID:"/>
            <Label Content="Имя:"/>
            <Label Content="Фамилия:"/>
            <Label Content="Отчество:"/>
            <Label Content="Специализация:"/>
        </StackPanel>
        <StackPanel Grid.Column="2" Grid.Row="1" HorizontalAlignment="Left">
            <TextBlock Margin="5" Text="{Binding DoctorInfo.ID}"/>
            <TextBlock Margin="5" Text="{Binding DoctorInfo.Name}"/>
            <TextBlock Margin="5" Text="{Binding DoctorInfo.LastName}"/>
            <TextBlock Margin="5" Text="{Binding DoctorInfo.MiddleName}"/>
            <TextBlock Margin="5" Text="{Binding DoctorInfo.Specialisation}"/>
        </StackPanel>

        <ListView Grid.Row="1" ItemsSource="{Binding Patients}" SelectedItem="{Binding SelectedPatient}" x:Name="listpat">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Идентификатор: "/>
                            <TextBlock Text="{Binding ID}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Имя: "/>
                            <TextBlock Text="{Binding Name}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Фамилия: "/>
                            <TextBlock Text="{Binding LastName}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Отчество: "/>
                            <TextBlock Text="{Binding MiddleName}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Номер телефона: "/>
                            <TextBlock Text="{Binding Number, StringFormat={}{0:# (###) ###-##-##}}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Дата рождения: "/>
                            <TextBlock Text="{Binding Birthday}"/>
                        </StackPanel>
                        <TextBlock x:Name="sover" Text="{Binding Sover}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>